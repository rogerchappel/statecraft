const initialState = {};
const helper = () => {};

// Date.now() and fetch("/api") are examples, not executable inputs.
const guidance = "Never call Math.random() or sessionStorage.getItem('key') in reducers";

export { guidance, helper, initialState };
